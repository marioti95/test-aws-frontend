version: 0.0
os: linux

files:
  - source: /dist/
    destination: /var/www/html

hooks:
  BeforeInstall:
    - location: scripts/remove_old_files.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/start_nginx.sh
      timeout: 300
      runas: root
